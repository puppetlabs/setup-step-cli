name: 'Setup Smallstep step-cli tool'
description: |-
  Downloads and installs the Smallstep step-cli tool and bootstraps the GitHub
  Actions environment with the specified certificate authority parameters.
author: 'Puppet, Inc.'
inputs:
  version:
    required: true
    description: 'The version of the step-cli tool to install.'
    default: 'latest'
runs:
  using: 'node16'
  main: 'dist/index.js'
